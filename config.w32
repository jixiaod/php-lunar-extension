// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("datetolunar", "for datetolunar support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("datetolunar", "enable datetolunar support", "no");

if (PHP_DATETOLUNAR != "no") {
	EXTENSION("datetolunar", "datetolunar.c");
}

